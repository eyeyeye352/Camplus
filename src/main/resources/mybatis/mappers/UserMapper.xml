<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.camplus.login.mappers.UserMapper">

    <resultMap id="UserResultMap" type="com.camplus.login.entity.User">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="email" property="email"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insertUser" parameterType="com.camplus.login.entity.User">
        INSERT INTO users (username, password_hash, email, role, create_time, update_time)
        VALUES (#{username}, #{passwordHash}, #{email}, #{role}, NOW(), NOW())
    </insert>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <update id="updateUsername">
        UPDATE users SET username = #{username}, update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateEmail">
        UPDATE users SET email = #{email}, update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updatePassword">
        UPDATE users SET password_hash = #{passwordHash}, update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="selectById" resultMap="UserResultMap">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <select id="selectByRole" resultMap="UserResultMap">
        SELECT * FROM users WHERE role = #{role} ORDER BY create_time DESC
    </select>

    <update id="updateRole">
        UPDATE users SET role = #{role}, update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser">
        DELETE FROM users WHERE user_id = #{userId}
    </delete>

</mapper>
