server:
  port: 8080

# Spring 数据源配置（Spring Boot 接管数据库，核心）
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/camplus_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 你的MySQL密码
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis 整合配置
mybatis:
  mapper-locations: classpath:mybatis/mappers/*.xml
  config-location: classpath:mybatis/mybatis-config.xml
  type-aliases-package: com.camplus.login.entity

langchain4j:
  open-ai:
    chat-model:
      # 【重要】请去智谱或DeepSeek官网免费申请一个API Key填在这里
      api-key: sk-rffxhncencqyeehwvkylxgddvrhxrbqiqrvyukqlyumhozqv
      # 这里以 DeepSeek 为例，如果是智谱请换成智谱的 baseUrl
      base-url: https://api.siliconflow.cn/v1
      model-name: Qwen/Qwen2.5-72B-Instruct
      temperature: 0.1
      log-requests: true
      log-responses: true